%nav.navbar.navbar-default{ role: 'navigation' }
  .container-fluid
    .navbar-header
      %button.navbar-toggle{ data: { target: '#navbar-kaddisto-airways', toggle: 'collapse' }, type: 'button' }
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{:href => "/"}
      = image_tag(asset_path('plane.png'), class: 'd-inline-block align-top')
    #navbar-kaddisto-airways.collapse.navbar-collapse
      %ul.nav.navbar-nav.navbar-center
        -unless user_signed_in? && current_user.admin?
          %li
            = link_to 'Бронирование', new_my_ticket_ticket_path(:my_ticket_id)
          %li
            = link_to 'Мои бронирования', my_ticket_tickets_path(:id)
      %ul.nav.navbar-nav.navbar-right
        - if  user_signed_in? && current_user.admin?
          %li
            = link_to 'Самолеты', aircrafts_path
          %li
            = link_to 'Аэропорты', airports_path
          %li
            = link_to 'Авиакомпании', airlines_path
          %li
            = link_to 'Добавить билет', new_ticket_path
        %li
          - if user_signed_in?

            = link_to 'Выход', destroy_user_session_path, method: :delete
          - else
            = link_to 'Войти', new_user_session_path