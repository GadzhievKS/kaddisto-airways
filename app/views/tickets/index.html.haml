.index-ticket
  %h1.text-center.title Билеты
  = link_to 'Добавить билет', new_ticket_path, class: 'new btn btn-primary'
  %table.table.table-hover
    %thead
      %th
      %th Откуда
      %th Куда
      %th Время
      %th Цена
    %tbody
      - @tickets.each do |ticket|
        %tr
          %td= ticket.id
          %td= ticket.from
          %td= ticket.to
          %td= ticket.time
          %td= ticket.price
